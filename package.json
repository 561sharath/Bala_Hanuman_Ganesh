{
  "name": "vinayaka-chanda-app",
  "version": "1.0.0",
  "private": true,
  "description": "Sri Bala Hanuman Vinayaka Seve Samithi collection portal web app",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server",
    "start": "node server/server.js",
    "dev": "npm run dev --prefix server"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
